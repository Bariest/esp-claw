# Build output
build/
build-*/
tmp/
releases/

# ESP-IDF
sdkconfig
sdkconfig.old
sdkconfig.priv
sdkconfig.bmgr_board.old
dependencies.lock
managed_components/

# Generated by `idf.py bmgr` -- never commit, always regenerate
components/gen_bmgr_codes/

# Frontend build artefacts (the gzipped output IS committed; node_modules is not)
node_modules/
components/http_server/frontend_source/dist/*
!components/http_server/frontend_source/dist/index.html.gz
components/http_server/frontend_pwa/node_modules/
# The PWA build writes both plain and gzipped variants into www/. Only the
# .gz are flashed onto the system partition, and those are committed so a
# fresh clone builds firmware without needing node installed.
components/http_server/frontend_pwa/www/*
!components/http_server/frontend_pwa/www/*.gz
components/http_server/frontend_pwa/.vite/

# IDE / Editor
.vscode/
.devcontainer/
.idea/
.clangd
*.swp
*.swo
*~

# Environment
.env
.venv/
.cache
*.pyc

# MCP / Memov
.mcp.json
.mem/
.memignore

# OS
.DS_Store
Thumbs.db

.codex
